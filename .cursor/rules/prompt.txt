

Tasks:
1 Create `src/components/Footer.tsx`
   Structure (desktop -> 4 columns; mobile -> stacked):
   - Column A: Brand
     - Logo text: "🐾 PetCare"
     - One-sentence placeholder: "A simple way to find nearby vets & shelters."
   - Column B: Product links (placeholder routes)
     - Home → "/"
     - About → "/about"
     - Story → "/story"
     - Add a Clinic → "/add-clinic"
     - Contact → "/contact"
   - Column C: Help & Legal
     - FAQ → "/faq" (placeholder)
     - Privacy → "/privacy" (placeholder)
     - Terms → "/terms" (placeholder)
     - Report an issue → "/contact"
   - Column D: Stay in touch (placeholders)
     - Email: "hello@petcare.example" (mailto link)
     - Social icon links (use simple text or inline SVGs): X/Twitter, Instagram, GitHub → "#"
     - Newsletter input (non-functional) with label "Email address" and a disabled "Subscribe" button

   Bottom bar (full width):
   - © {currentYear} PetCare. "Made with ❤️ for pets." (placeholder)
   - Locale/language select (static <select> with "EN" only for now)

   Accessibility:
   - Use semantic <footer> and <nav aria-label="Footer"> for link groups
   - Each column has an <h2> or visually hidden heading for screen readers
   - Inputs have labels; icons have aria-labels or titles
   - Focus-visible outlines preserved; links have :focus styles

   Styling:
   - Light background, subtle top border, comfortable padding (py-10)
   - Grid: responsive (e.g., 1 → 2 → 4 columns)
   - Bottom bar with thinner padding (py-4), border-top
   - Supports dark mode if Tailwind present (e.g., dark:bg-neutral-900, dark:text-neutral-200)

   Props:
   - `className?: string`

2) Wire globally:
   - Import and render <Footer /> in `app/layout.tsx` below <main>
   - Ensure the layout is: <body><NavBar /><main>{children}</main><Footer /></body>
   - Add top padding to <main> if needed to clear the sticky header
   - Keep Footer client-free (no 'use client' unless necessary)

3) Create placeholder pages if missing (view only, minimal):
   - `app/faq/page.tsx`    → H1 "FAQ" + short placeholder
   - `app/privacy/page.tsx`→ H1 "Privacy Policy" + placeholder
   - `app/terms/page.tsx`  → H1 "Terms of Service" + placeholder

4) DX & fallback:
   - If Tailwind is not configured, create `src/components/Footer.module.css` with minimal styles and import it in the component
   - No external data calls; all content is static placeholders

Acceptance Criteria:
- Footer renders on every route, below content
- Responsive columns collapse gracefully on small screens
- Links navigate to existing or placeholder pages
- Newsletter input is labeled, disabled, and keyboard accessible
- Social links have aria-labels
- Lighthouse a11y ≥ 90 on a Footer-only page
